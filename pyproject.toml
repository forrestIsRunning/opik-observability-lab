[project]
name = "opik-for-ob"
version = "0.1.0"
description = "Opik LLM Observability experiments"
requires-python = ">=3.11, <3.13"
dependencies = [
    "opik>=1.0.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]